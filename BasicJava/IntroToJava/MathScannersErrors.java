import java.util.InputMismatchException;
import java.util.Scanner;

public class MathScannersErrors {
    public static void main(String[] args) {
        //How do we get input?

        //New notation: Object creation -> similar but different to primitive creation
        //[OBJECT_NAME] [YOUR_OBJECT_NAME] = new [OBJECT_NAME]([PARAMETERS]);
        /*
        Scanner methods (think back to string methods)
            -hasNext() Returns true if the next token matches the pattern constructed from the specified string.
            -hasNextInt() Returns true if the next token in this scanner's input can be interpreted as an int value in the default radix using the nextInt() method.
            -hasNextDouble() Returns true if the next token in this scanner's input can be interpreted as a double value using the nextDouble() method.
            -hasNextBoolean() Returns true if the next token in this scanner's input can be interpreted as a boolean value using a case insensitive pattern created from the string "true|false".
            
            -next() Finds and returns the next complete token from this scanner (USES DELIMITERS). String
            -nextInt() Scans the next token of the input as an int. Int
            -nextDouble() Scans the next token of the input as a double. Double
            -nextBoolean() Scans the next token of the input into a boolean value and returns that value. Boolean

            -nextLine() Advances this scanner past the current line and returns the input that was skipped.

            -useDelimiter() Sets this scanner's delimiting pattern to the specified pattern.
        */
        //Issues with the specific reads
        //Try/Catch!
        //Math operators (+. -. *, /, %)
    }
}